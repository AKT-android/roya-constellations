import 'package:flutter/material.dart'

class RoyaColors {
  // Deep space backgrounds
  static const Color deepSpace = Color(0xFF020D1A);
  static const Color navyPrimary = Color(0xFF041525);
  static const Color navySecondary = Color(0xFF071E33);
  
  // Card and surface
  static const Color cardSurface = Color(0xFF0B2945);
  static const Color elevatedSurface = Color(0xFF103A5A);
  
  // Teal accent
  static const Color tealPrimary = Color(0xFF00A9A5);
  static const Color tealBright = Color(0xFF2DD4BF);
  static const Color blueAccent = Color(0xFF168DE2);
  
  // Lime/Yellow from logo
  static const Color limeAccent = Color(0xFFFFFF00);
  static const Color limeYellow = Color(0xFFFDD835);
  
  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA8C3D5);
  
  // Borders and dividers
  static const Color border = Color(0xFF1B4865);
  static const Color divider = Color(0xFF0F3A52);
}

class RoyaSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 20.0;
  static const double xxl = 24.0;
}

class RoyaBorderRadius {
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 20.0;
}
